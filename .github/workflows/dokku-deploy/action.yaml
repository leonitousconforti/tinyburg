name: Reusable dokku deploy workflow

inputs:
  DOKKU_HOST:
    required: true
  DOKKU_USER:
    required: true
  DOKKU_KEY:
    required: true
  DOKKU_APP:
    required: true

runs:
  using: "composite"
  steps:
    - name: "placeholder"
      shell: bash
      run: echo "placeholder"

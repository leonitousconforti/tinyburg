name: CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/build.yaml

  deploy-authproxy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Hi Mom
        run: echo "Hi, Mom!"

  deploy-auto-gold-bits:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Hi Mom
        run: echo "Hi, Mom!"

  deploy-discord-bot:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Hi Mom
        run: echo "Hi, Mom!"

  deploy-repl-demo:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Hi Mom
        run: echo "Hi, Mom!"

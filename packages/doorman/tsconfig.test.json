{
    "$schema": "https://json.schemastore.org/tsconfig",
    "extends": "./tsconfig.json",
    "include": ["test/**/*.ts"],
    "compilerOptions": {
        "typeRoots": ["node_modules/@types", "./test"],
        "types": ["node", "mocha", "chai", "typings"]
    }
}

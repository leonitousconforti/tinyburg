# @tinyburg/treasury-sdk

The treasury's api spec: player-to-player trades, escrowed. Consumed by
`apps/treasury` (the server) and `apps/tinyburg.app` (the `/trades` page and
the `/v1/treasury/*` reverse proxy).
